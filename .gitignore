# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
backend/openapi.json

# Node
node_modules/
dist/
*.tsbuildinfo
# frontend/src/api/schema.d.ts is committed on purpose — it's the frontend's
# typed API contract (ADR-0003, ADR-0005) and CI diffs a regenerated copy
# against it to catch drift; `make gen-api` regenerates it from the backend.

# SAM
.aws-sam/

# Env
.env
.env.local

# OS
.DS_Store
